<!--
用途: 车辆/轮子类任务可选规则包（routing 命中时可注入）
-->

## 车辆约定
- 世界：右=+X，前=-Y，上=+Z；前后用 ±Y，左右用 ±X
- **车轮必须立着**：`cad.cylinder(..., rot_y=90)`（轴沿左右 X）。漏 rot → 轮子躺平
- 验收：轮 bbox 最薄维 ≈ 轮宽；贴地 `zmin≈0`
- 侧壁孔/轮拱切割：刀轴或 `cad.hole(..., axis="X"|"Y")` 与面法向一致，勿默认 Z
