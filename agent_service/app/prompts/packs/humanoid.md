<!-- 用途: 人形/双足角色规则包 -->

## 任务规则：人形 / 双足
- 左右肢体用 ±X（`right`/`left`）；前后用 ±Y；高度用 Z
- 声明「脚/底在 z=0」则支撑件 `zmin≥0`
- 左右件中心 `|X|` 应明显大于躯干半宽，禁止贴在「前脸」上冒充侧面
- 完成前：对称件两侧均可见；对侧中心 `|X|` 相等、符号相反
- 部件多（约 >5）：先骨架（主体定位 + 接口），再逐个零件
