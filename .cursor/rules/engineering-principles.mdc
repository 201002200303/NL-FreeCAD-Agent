---
description: 工程原则——不兼容旧路径、简单实现、增量交付、模块化、优先成熟库与已验证模式
alwaysApply: true
---

# 工程原则

## 1. 不保留向后兼容

过时 API、旧路径、废弃分支直接删除。禁止兼容层、migration、静默 fallback、「万一还有人用」的双路径。

## 2. 最简单实现

只做能满足当前明确需求的最小方案。禁止预防性抽象、多余配置层、为假想扩展预留的接口。

## 3. 增量变长

先跑通一个最小端到端版本，再叠加能力。禁止为未完成的复杂度拆掉已能跑的路径。

## 4. 模块化与关注点分离

组件边界清晰：契约、执行、审查、UI 各司其职，避免上帝模块与跨层偷传状态。

## 5. 优先成熟库

优先选用有维护的库。没有明确理由（缺失能力、许可、体积、运行环境限制）不要自研轮子。

## 6. 先查现有依赖

加新包或手写之前，先确认项目已有依赖能否覆盖。不要假设「库里一定没有」。

## 7. 架构决策做长线

拒绝「先这样以后再换」的临时架构。短期可砍范围，不可埋注定要推倒的结构债。

## 8. 先看成熟产品怎么做

同一问题先找已验证模式（开源 CAD agent、成熟测试/审查流程等），再落地；禁止无参照地从零发明流程。

## 反例（本项目语境）

```text
❌ 为旧 Plan API 留 fallback；新 move 映射同时保留「忽略位置参」行为
❌ 未跑通单工具审查就上完整评测平台 / 多后端配置
❌ 自研截图队列，而不看 FreeCAD/现有 viewport 能否复用
✅ 删掉过时路径；最小 runner + 人工 Pass/Fail；用现有 cad_program 执行用例
```
